# 联网搜索与思考折叠栏技术实现详解

> 文档名称：联网搜索与思考折叠栏技术实现详解
> 适用范围：当前项目的 DeepSeek API 联网搜索、深度思考流式输出、AI 消息折叠栏、思考句子预览动画
> 代码基线：以本文档生成时项目工作区中的实际实现为准
> 主要代码：[app/api.py](app/api.py)、[app/worker.py](app/worker.py)、[app/ui/main_window.py](app/ui/main_window.py)、[app/ui/message_bubbles.py](app/ui/message_bubbles.py)

本文档不是产品使用说明，而是对当前实现的业务和关键代码进行逆向整理。重点回答以下问题：

- 联网搜索请求到底如何组装，模型和客户端各自负责什么；
- SSE、reasoning、content、tool_calls 如何解析和分流；
- 搜索服务商、网页抓取、来源去重和引用编号如何处理；
- 后台 Worker 如何把 API 事件传给主线程；
- 折叠栏如何在“正在深度思考”和“正在联网搜索”之间交替显示；
- 思考预览如何从流式内容提取完整句子，并按 0.5 秒打字、0.5 秒静止、60fps 的节奏动画；
- 如何避免半句话、工具协议、搜索前缀和超长文字污染用户界面；
- 请求完成、停止、失败、历史恢复和主题切换时，相关控件如何收尾。

---

## 1. 结论先行

当前实现可以概括为四层：

1. **API 编排层**：`app/api.py` 使用 Chat Completions 的 `tools` / `tool_calls` 能力，把联网搜索建模为客户端可执行的函数工具。
2. **后台线程层**：`app/worker.py` 在 `QThread` 中执行网络请求、SSE 读取和本地搜索，向主线程发出 `reasoning`、`status`、`chunk` 等 Qt 信号。
3. **会话与状态层**：`app/ui/main_window.py` 用 `StreamContext` 累积当前回答的正文和思考内容，把后台事件转发给当前 `AssistantBubble`。
4. **折叠栏展示层**：`app/ui/message_bubbles.py` 的 `ReasoningPanel` 负责折叠交互、运行态标题和思考预览；`ThinkingPreview` 负责完整句子提取、1 秒循环以及 60fps 打字动画。

最重要的设计判断如下：

- 联网搜索不是 UI 直接调用搜索服务，而是**模型先决定是否调用工具**；客户端负责执行模型请求的工具调用。
- “正在深度思考”和“正在联网搜索”不是两个并行面板，而是同一个 `ReasoningPanel` 的两个运行阶段。
- 两个阶段的交替由 API 流中的事件驱动，不是 UI 自己按照固定时间切换标题。
- 思考预览只取 `reasoning_content` / `reasoning`，不会把最终回答、工具参数或搜索结果直接当作思考内容显示。
- 预览文字只接受已经出现句末标点的完整句子，当前尚未结束的流式尾部会被留在缓冲中。
- 完成态仍保留右侧可伸缩预览槽位，只清空文字和停止动画，保证图标与“已完成深度思考”不会因 Qt 布局重新分配而跳到中央。

---

## 2. 代码地图与职责边界

| 文件 | 关键对象/函数 | 职责 |
| --- | --- | --- |
| `app/api.py` | `SEARCH_TOOL`、`SEARCH_TOOLS` | 定义模型可调用的搜索、打开网页工具 |
| `app/api.py` | `build_chat_body()` | 组装 DeepSeek 请求体，控制思考、温度、工具和 `tool_choice` |
| `app/api.py` | `_iter_chat_payloads()` | 读取普通 JSON 或 SSE，并解析 `data:` 数据行 |
| `app/api.py` | `_extract_parts()` | 从模型响应中分离思考和回答，清理泄漏的内部标签 |
| `app/api.py` | `stream_chat()` | 普通流式回答与联网搜索流的统一入口 |
| `app/api.py` | `stream_search_function()` | 多轮工具调用、搜索执行、来源编号、最终回答生成 |
| `app/api.py` | `run_web_search()` | 根据配置选择 DuckDuckGo 或 Tavily |
| `app/api.py` | `fetch_page_text()` | 打开网页、提取标题和正文、限制内容大小 |
| `app/worker.py` | `ChatWorker` | 在 Qt 后台线程中消费 API 生成器并发出信号 |
| `app/ui/main_window.py` | `StreamContext` | 保存当前请求的会话 ID、正文、思考、模型和状态 |
| `app/ui/main_window.py` | `MainWindow._on_reasoning()` | 累积思考流并更新气泡 |
| `app/ui/main_window.py` | `MainWindow._on_status()` | 把搜索阶段状态转发给气泡 |
| `app/ui/main_window.py` | `MainWindow._on_chunk()` | 累积正文并切换到回答阶段 |
| `app/ui/message_bubbles.py` | `ReasoningPanel` | 折叠栏、运行态阶段和折叠内容 |
| `app/ui/message_bubbles.py` | `ThinkingIndicator` | 思考/搜索运行中的旋转指示图标 |
| `app/ui/message_bubbles.py` | `ThinkingPreview` | 思考句子提取、文字绘制、右侧渐变和动画 |
| `app/ui/message_bubbles.py` | `AssistantBubble` | 拼装元数据、折叠栏、状态行、正文和操作按钮 |
| `app/ui/theme.py` | `build_qss()`、`colors()` | 折叠栏背景、状态文字和预览文字的主题样式 |
| `app/config.py` | `DEFAULTS`、`_sanitize()` | 联网搜索默认配置和配置边界校验 |
| `app/ui/settings_dialog.py` | 搜索设置控件 | 默认开关、服务商、Tavily Key 和结果数配置 |
| `tests/test_api.py` | 搜索流、工具调用和来源测试 | 验证 API 编排和数据格式 |
| `tests/test_ui.py` | 折叠栏、状态切换和动画测试 | 验证 UI 状态、句子动画和布局稳定性 |

### 2.1 关键源码定位

以下行号是本文生成时工作区的快照，后续插入代码后可能变化；函数名和类名是更稳定的检索入口。

| 关注点 | 当前源码入口 |
| --- | --- |
| 请求体与 reasoning/content 分流 | app/api.py:262 的 build_chat_body；app/api.py:300 的 _extract_parts |
| 搜索服务与网页正文 | app/api.py:346 的 run_web_search；app/api.py:540 的 fetch_page_text |
| SSE 和多轮工具循环 | app/api.py:707 的 _iter_chat_payloads；app/api.py:747 的 stream_chat；app/api.py:802 的 stream_search_function |
| Worker 信号与停止 | app/worker.py:10 的 ChatWorker |
| 会话启动与事件转发 | app/ui/main_window.py:548 的 StreamContext；app/ui/main_window.py:1129 的 on_send；app/ui/main_window.py:1256 起的三个实时槽函数 |
| 图标、句子、预览、面板 | app/ui/message_bubbles.py:804 的 ThinkingIndicator；895 的 _extract_complete_sentences；938 的 ThinkingPreview；1120 的 ReasoningPanel |
| 助手气泡和消息列表 | app/ui/message_bubbles.py:1367 的 AssistantBubble；1748 的 ChatView |

建议阅读顺序是：先看 stream_chat/stream_search_function，理解 API 事件边界；再看 ChatWorker 和 MainWindow 的信号接线；最后看 AssistantBubble、ReasoningPanel、ThinkingPreview 的状态和绘制。这样可以避免把搜索业务状态、消息生命周期和纯视觉动画混在一起理解。

---

## 3. 一次请求的端到端数据流

```text
用户点击发送
    │
    ▼
MainWindow.on_send()
    ├─ 读取深度思考、思考强度、联网搜索开关
    ├─ 保存 user message 和本轮元数据
    └─ _start_request()
          │
          ├─ 创建 AssistantBubble(streaming=True)
          ├─ 创建 StreamContext
          └─ 启动 ChatWorker(QThread)
                │
                ▼
          api.stream_chat()
                │
                ├─ web_search=False
                │     └─ 单次 Chat Completions 流
                │
                └─ web_search=True
                      └─ stream_search_function()
                            ├─ 模型流：reasoning / content / tool_calls
                            ├─ 本地执行 web_search / open_page / web_fetch
                            ├─ 生成 tool message
                            ├─ 继续下一轮模型请求
                            └─ 最终无工具回答流
                │
                ▼
          ChatWorker 信号
          ├─ reasoning(str)
          ├─ status(str)
          ├─ chunk(str)
          ├─ done()
          └─ failed(str)
                │
                ▼
          MainWindow 槽函数
          ├─ _on_reasoning()
          ├─ _on_status()
          ├─ _on_chunk()
          ├─ _on_done()
          └─ _on_failed()
                │
                ▼
          AssistantBubble
          ├─ ReasoningPanel
          │    ├─ ThinkingIndicator
          │    ├─ reasonToggle
          │    └─ ThinkingPreview
          ├─ status_row（标准模式使用）
          └─ RichText（最终回答）
```

### 3.1 线程边界

网络、SSE 和本地搜索都运行在 `ChatWorker.run()` 所在的 `QThread` 中。UI 控件不直接访问 `requests.Response`，也不在主线程执行搜索服务请求。

线程之间只通过 Qt 信号传递字符串：

| 后台生成器事件 | `ChatWorker` 信号 | UI 处理 |
| --- | --- | --- |
| `("reasoning", text)` | `reasoning.emit(text)` | 累积到 `StreamContext.reasoning` |
| `("search", text)` | `status.emit(text)` | 更新折叠栏运行阶段或标准模式状态行 |
| `("content", text)` | `chunk.emit(text)` | 累积到 `StreamContext.content`，开始显示回答 |
| 生成器正常结束 | `done.emit()` | 保存 assistant message，结束动画 |
| `APIError` 或其他异常 | `failed.emit(message)` | 保存部分结果或显示错误气泡 |

`ChatWorker` 内部还通过 `response_callback` 保存当前 `requests.Response`。点击停止时先设置 `threading.Event`，再关闭当前 Response，从而尽快打断 `iter_lines()` 或搜索请求。

---

## 4. API 请求如何开启联网搜索

### 4.1 配置来源

联网搜索有两类开关：

1. **设置页默认值**：`config.DEFAULTS["web_search"]` 默认是 `True`。
2. **当前输入面板开关**：`InputPanel.search_button` 是可勾选按钮，发送时通过 `is_web_search()` 读取。

设置页还保存：

- `search_provider`：`duckduckgo` 或 `tavily`；
- `search_api_key`：Tavily Key；
- `search_max_results`：最终会被限制在 1 到 10 之间。

`MainWindow.on_send()` 读取当前开关后，把 `web_search` 同时放进：

- `StreamContext.web_search`，用于当前请求和最终持久化；
- user message 的元数据，便于历史记录恢复元信息；
- `_message_meta()` 的显示字符串，例如 `深度思考 Max · 联网搜索`；
- `ChatWorker` 构造参数。

### 4.2 请求体的组装

所有模型请求都经过 `build_chat_body()`。基础字段如下：

```python
{
    "model": model,
    "messages": messages,
    "stream": True,
    "thinking": {
        "type": "enabled" if deep_thinking else "disabled"
    },
}
```

根据模式追加的字段：

| 条件 | 字段 | 行为 |
| --- | --- | --- |
| `deep_thinking=True` | `reasoning_effort` | 只接受 `low`、`high`、`max`，非法值回退 `high` |
| `deep_thinking=False` | `temperature` | 使用标准模式温度 |
| `max_tokens > 0` | `max_tokens` | 覆盖模型默认最大输出 |
| `web_search=True` | `tools` | 放入 `SEARCH_TOOLS` |
| `web_search=True` | `tool_choice` | 由调用者传入，否则根据模式推导 |

联网工具定义在 `app/api.py`：

```python
SEARCH_TOOL = {
    "type": "function",
    "function": {
        "name": "web_search",
        "description": "检索互联网上的最新信息……",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {"type": "string"}
            },
            "required": ["query"],
            "additionalProperties": False,
        },
    },
}
```

实际 `SEARCH_TOOLS` 不只有搜索：

| 工具名 | 作用 | 客户端执行函数 |
| --- | --- | --- |
| `web_search` | 根据关键词搜索网页 | `run_web_search()` |
| `open_page` | 打开一个指定 URL 并读取正文 | `fetch_page_text()` |
| `web_fetch` | `open_page` 的同义工具 | `fetch_page_text()` |

这意味着模型可以先搜索，再根据搜索结果要求客户端打开具体页面；当前客户端对这三种工具都保留了统一的工具消息回传路径。

### 4.3 `tool_choice` 的实际策略

`build_chat_body()` 的默认策略是：

- 深度思考模式 + 联网搜索：默认 `"auto"`，模型自行决定是否搜索；
- 标准模式 + 联网搜索：默认强制调用 `web_search`；
- 搜索多轮的第一轮：由 `stream_search_function()` 传入 `None`，让 `build_chat_body()` 根据模式决定；
- 搜索多轮的后续轮：显式传入 `"auto"`，允许模型决定继续搜索、打开网页，还是直接回答。

因此：

- 深度思考开启时，勾选联网搜索不等于每次一定发出搜索调用，模型可以判断已有知识足够；
- 标准模式开启联网搜索时，第一轮会强制要求 `web_search`，确保请求真的经过搜索；
- 后续轮不再强制只能搜索，否则模型无法自然结束工具循环。

输入面板的搜索按钮 tooltip 当前写的是“使用 DeepSeek Responses API 联网搜索”，但实际代码请求的是 `/{chat/completions}`，并使用 Chat Completions 的 `tools` / `tool_calls` 结构。若后续要对外说明接口类型，应以 `app/api.py` 的实际实现为准，或同步修正 tooltip 文案。

---

## 5. SSE 流式响应如何解析

### 5.1 HTTP 请求和响应头

`_chat_headers()` 生成：

```text
Authorization: Bearer <api_key>
Content-Type: application/json
Accept: text/event-stream
```

请求地址由 `_endpoint(cfg, "chat/completions")` 拼接，默认基地址来自配置。

### 5.2 `_iter_chat_payloads()` 的行为

该函数同时兼容两类响应。

#### 普通 JSON

如果 `Content-Type` 包含 `application/json`：

1. 检查 HTTP 状态码是否为 200；
2. 直接调用 `response.json()`；
3. JSON 无法解析时抛出 `APIError("DeepSeek 返回了无效 JSON")`；
4. 产出一个完整 payload 后结束。

#### SSE

其他响应按 `response.iter_lines(decode_unicode=True)` 逐行读取：

1. 检查停止事件；
2. 去除首尾空白；
3. 只处理以 `data:` 开头的行；
4. 遇到 `data: [DONE]`（大小写不敏感）结束当前流；
5. 空数据行跳过；
6. 单行 JSON 解码失败时跳过该行，避免一个异常 chunk 直接破坏整个回答。

`_iter_chat_payloads()` 通过 `with response:` 管理连接；普通流结束、错误和停止都会退出上下文。外层 `stream_chat()` 和 `stream_search_function()` 的 `finally` 再通过 `response_callback(None)` 清空 Worker 保存的当前响应。

### 5.3 `_extract_parts()` 如何分离思考和回答

每个 `choice` 优先取：

```python
message = choice.get("delta") or choice.get("message") or {}
```

这样既支持流式 `delta`，也支持某些兼容网关用完整 `message` 返回的形式。

字段兼容关系：

| 语义 | 首选字段 | 备用字段 |
| --- | --- | --- |
| 思考 | `reasoning_content` | `reasoning` |
| 回答 | `content` | 无 |

拿到文字后会经过 `_strip_zenith()`：

- 删除 `<zenith_parameter>`、`<zenith_invoke>`、`<zenith_calls>` 等内部格式块；
- 删除紧邻这些内部块之前的 `reasoning` / `response` 标记；
- 普通文本中的 `response time` 等词不会被误删；
- 清理后的空字符串不会继续向上游发送。

最后产出统一事件：

```python
("reasoning", cleaned_reasoning)
("content", cleaned_content)
```

同一个 payload 中如果同时存在思考和正文，当前实现先产出 reasoning，再产出 content。这对 UI 很重要：思考内容先进入折叠栏，正文到达时再切换到回答展示。

---

## 6. 联网搜索的多轮工具调用编排

### 6.1 搜索入口不是“搜索后直接拼答案”

`stream_chat()` 是统一入口：

```python
if web_search:
    yield from stream_search_function(...)
    return
```

因此，打开联网搜索后仍然使用同一个模型对话接口，只是把一次请求扩展成一个客户端控制的工具循环。搜索结果不会直接在 UI 中独立渲染成搜索卡片；它们先变成 `role="tool"` 的上下文，交给模型继续生成最终回答。

这条链路可以抽象成：

```text
用户消息
  │
  ▼
模型第 N 轮请求（携带 tools）
  │
  ├─ reasoning_content ───────────────► 立即向 UI 发送思考
  │
  ├─ content（工具调用前缀）──────────► 暂存，不立即当成最终答案
  │
  └─ tool_calls ─────────────────────► 客户端执行工具
                                         │
                                         ├─ web_search
                                         └─ open_page / web_fetch
                                                │
                                                ▼
                                     生成 role=tool 消息
                                                │
                                                ▼
                                   模型下一轮请求继续判断
                                                │
                         没有新的 tool_calls
                                                ▼
                                    输出最终 content
```

### 6.2 每一轮的处理顺序

`stream_search_function()` 为每个请求复制一份 `working = list(messages)`，避免直接改写主窗口传入的历史消息。之后最多执行 `MAX_SEARCH_ROUNDS = 3` 个带工具的模型轮次。

每一轮内部按以下顺序处理：

1. 用当前 `working` 构造带 `tools` 的 Chat Completions 请求；
2. 逐个读取 SSE payload；
3. 立即转发 `reasoning`，把 `content` 暂存在 `round_content`；
4. 把流式分片中的 `tool_calls` 按 `index` 合并；
5. 响应结束后判断本轮是否出现工具调用；
6. 没有工具调用：把暂存正文按原顺序产出，整个搜索流程完成；
7. 有工具调用：先发出搜索状态，再按调用顺序执行工具；
8. 把本轮 assistant 工具调用消息和每个 `role="tool"` 结果追加到 `working`；
9. 进入下一轮。

其中第 3 步是一个关键的展示策略。模型经常会先返回“我先搜索一下”之类的短前缀，然后才返回真正的工具调用。如果一收到 `content` 就显示，用户会先看到一段看似最终答案的前缀，随后又被搜索状态打断。当前代码因此只在确认“本轮没有 `tool_calls`”时才释放 `round_content`。

思考流不受这个延迟策略影响：只要 `_strip_zenith()` 清理后仍有内容，就会在本轮实时 `yield ("reasoning", text)`。所以在模型决定搜索、等待搜索结果和继续生成之间，折叠栏仍然可以持续展示思考。

### 6.3 流式 `tool_calls` 的合并

兼容的模型网关通常会把一次函数调用拆成多个 delta，例如：

```text
第一个分片：{"index": 0, "id": "call_1", "function": {"name": "web_search"}}
第二个分片：{"index": 0, "function": {"arguments": "{\"query\":\"北京天气"}}
第三个分片：{"index": 0, "function": {"arguments": "\"}"}
```

当前实现以调用索引为 key，维护：

```python
tool_calls[index] = {
    "id": "",
    "name": "",
    "arguments": "",
}
```

收到分片后分别更新 `id`、`name`，并将 `function.arguments` 追加到原字符串末尾。响应结束后再一次性 `json.loads()`，避免在 JSON 还没接收完整时过早执行搜索。

多工具并行调用时，最终按照 `sorted(tool_calls)` 的索引顺序执行和组装消息，保证模型看到的工具调用顺序稳定。

### 6.4 每轮状态文本

当前工具循环向 `ChatWorker` 发送的状态文本为：

| 时机 | API 事件 | UI 含义 |
| --- | --- | --- |
| 已检测到一轮或多轮 tool call，准备执行 | `("search", "正在联网搜索…")` | 折叠栏进入联网搜索阶段 |
| 至少注册到一个来源，准备让模型生成 | `("search", "已获取 N 条结果，正在生成回答…")` | 折叠栏仍处于搜索/检索阶段，但文案提示已转入生成 |
| 工具调用没有得到任何来源 | `("search", "未找到可用结果，正在生成回答…")` | 提示搜索为空，仍继续让模型回答 |

UI 当前根据状态字符串是否以“正在联网搜索”开头来识别搜索阶段。由于 `AssistantBubble.set_status()` 对不以“正在联网搜索”开头的状态统一归为 `THINKING_PHASE`，所以“已获取…正在生成回答…”和“未找到可用结果…”这两条状态在带思考面板时会把标题恢复为“正在深度思考”；标准模式则显示“正在生成”。这是当前字符串兼容逻辑的实际结果。如果希望标题准确表达“正在生成回答”，应扩展显式 phase 枚举/状态事件，而不是继续增加字符串前缀分支。

### 6.5 工具结果如何回填模型上下文

有工具调用时，客户端先追加 assistant 消息。消息保留：

- 本轮暂存的 `content`，通常是工具调用前缀；
- 本轮已流出的 `reasoning_content`；
- 按索引排序的完整 `tool_calls`，包括 `id`、`type="function"`、函数名和完整参数。

随后为每个调用追加：

```python
{
    "role": "tool",
    "tool_call_id": call_id,
    "content": "...编号后的搜索结果或网页正文...",
}
```

成功时 `content` 由 `sources_tool_content()` 生成；异常时只回传前两条错误，用“工具执行失败：”作为前缀；没有结果且没有异常时回传“本次操作没有返回可用结果。”。这样模型可以自行决定是否换关键词、打开其他页面或基于已有结果作答。

### 6.6 达到轮数上限后的收尾

如果连续三轮都产生工具调用，代码不会继续无限搜索。它会在 `working` 末尾添加一条内部 system 消息：

```text
请基于已经获取的全部信息直接回答用户的问题，不要再调用任何工具，
也不要输出 <zenith_calls>、<zenith_invoke> 等内部格式。
```

然后发起一次不带 `web_search` 的最终流式请求。此请求只负责将已有的搜索/网页结果综合为答案，最终产出的 reasoning/content 仍通过 `_extract_parts()` 统一发送给 Worker。

这条收尾规则同时控制了两个风险：

- 搜索次数有硬上限，避免模型因工具调用循环造成请求失控；
- 工具协议已经完成后，最终回答不会再次生成内部调用标记。

### 6.7 停止和异常

工具循环的每个关键节点都检查 `stop.is_set()`：发请求前、读取每个 payload 后、执行每个查询/URL 前以及回填消息前均可提前返回。用户点击停止时不会再启动下一轮搜索。

网络异常统一转换为 `APIError("网络请求失败：...")`。搜索服务或网页抓取的单个调用失败时，错误会作为该调用的 tool message 回传给模型；如果是模型请求本身失败，则由 Worker 统一转成 `failed` 信号。

---

## 7. 搜索服务、网页读取和安全边界

### 7.1 搜索服务商选择

`run_web_search(cfg, query)` 只接受一个已清理的查询文本，并根据 `cfg["search_provider"]` 分流：

| 配置值 | 服务 | 鉴权 | 主要特点 |
| --- | --- | --- | --- |
| `duckduckgo` 或未知值 | DuckDuckGo Lite | 不需要 Key | 通过 HTML 页面解析结果 |
| `tavily` | Tavily Search API | 必须有 `search_api_key` | 通过 JSON API 获取结果 |

空查询会直接返回空列表，不发网络请求。结果数读取 `search_max_results`，转换失败时使用默认值，最终强制夹在 1 到 10 之间。

### 7.2 Tavily 请求

`_tavily_search()` 向 `https://api.tavily.com/search` 发送 JSON：

```json
{
  "api_key": "<configured key>",
  "query": "<user/model query>",
  "max_results": 5,
  "search_depth": "basic",
  "include_answer": false
}
```

请求连接/读取超时为 `(5, 30)` 秒。响应必须是 HTTP 200 且能解析为 JSON。每条结果统一压缩为：

```json
{
  "title": "...",
  "url": "...",
  "snippet": "..."
}
```

这里的 `snippet` 实际来自 Tavily 的 `content` 字段；缺少三项全部内容的条目会被丢弃。

### 7.3 DuckDuckGo Lite 请求和解析

`_duckduckgo_search()` 向 `https://lite.duckduckgo.com/lite/` 发送表单字段 `q`，带浏览器 User-Agent 和 HTML Accept 头，请求超时为 `(5, 20)` 秒。

解析过程不是依赖浏览器 DOM，而是使用正则识别：

- 包含 `result-link` 的 `<a>` 元素；
- `class="result-snippet"` 的摘要单元格；
- DuckDuckGo 的 `/l/?uddg=...` 跳转链接。

`_ddg_target()` 会 URL 解码 `uddg`，必要时补齐 `https:` 协议。解析后的标题、摘要会先去除 HTML 标签、实体解码并压缩空白；同一 URL 在本次结果中只保留一次，并截断到配置的最大条数。

这种解析方式依赖 DuckDuckGo Lite 当前 HTML 结构，优点是无需额外 Key，缺点是页面结构变动会影响结果提取。代码对解析失败采取空结果路径，由上层继续把“没有可用结果”反馈给模型。

### 7.4 `open_page` / `web_fetch` 的网页读取

网页工具不把原始 HTML 直接交给模型，而是先转成可读文本。`fetch_page_text()` 的处理流程是：

1. 去除 URL 两端空白；
2. 只允许 `http` / `https` 且必须有 netloc；
3. 拒绝 `localhost`、`127.0.0.1`、`0.0.0.0`、`::1` 和 `.localhost`；
4. 使用浏览器 User-Agent 发起流式 GET；
5. 只接受 Content-Type 中包含 `html` 或 `text` 的响应；
6. 最多读取 2 MiB 原始字节，避免单页响应无限增长；
7. 使用响应声明的编码解码，失败时回退 UTF-8 replacement；
8. 通过 `_PageTextExtractor` 删除脚本、样式、noscript、template、svg；
9. 采集 title 和可见文本；
10. 文本超过 `MAX_PAGE_TEXT_CHARS = 12_000` 时截断并追加省略号。

最终返回的页面记录仍使用统一格式：

```python
{
    "title": extractor.title() or parsed.netloc,
    "url": target,
    "snippet": readable_text,
}
```

### 7.5 当前安全限制与可改进点

现有实现已经避免最明显的回环地址读取，但它不是完整的 SSRF 防护方案。尤其需要注意：

- `requests.get()` 默认允许重定向，当前只校验初始 URL，没有逐跳校验重定向目标；
- 只拦截显式回环主机名/IP，没有对解析后的私网、链路本地、保留地址做完整 IP 检查；
- URL 没有额外的端口白名单；
- HTML 解析是静态文本提取，不执行 JavaScript，因此依赖前端渲染的页面可能只有空壳文本；
- 2 MiB 是读取上限，`max_chars` 是文本输出上限，两者控制的是不同阶段。

如果未来将网页读取暴露给不可信模型或更广泛的网络环境，建议把重定向逐跳校验、DNS 解析后的 IP 分类、端口策略和响应体限制作为独立的网络安全层实现，并配套测试；不能只依赖当前 `_is_loopback_url()`。

---

## 8. 搜索结果去重、编号和引用约束

### 8.1 统一的来源记录

不论结果来自 Tavily、DuckDuckGo 还是网页正文，进入工具上下文前都被归一为 `title`、`url`、`snippet` 三个字段。这样模型不需要知道底层服务商差异，来源编号也可以跨工具类型复用。

`stream_search_function()` 内部维护：

```python
sources: list[dict] = []
source_numbers: dict[str, int] = {}
```

注册规则：

1. 有 URL：以 URL 作为去重 key；
2. 没有 URL：以 `title|snippet` 作为 key；
3. key 已存在：返回原编号，不重复追加；
4. 新来源：追加到 `sources`，编号为当前列表长度。

因此，同一个页面先出现在搜索结果、后又被 `open_page` 读到时，不会获得两个不同编号；新的网页来源会继续使用全局递增编号。

### 8.2 工具上下文的格式

`format_search_results()` 和 `sources_tool_content()` 使用如下语义格式：

```text
[1] 页面标题
URL: https://example.com/a
摘要: 页面可读文本或搜索摘要

[2] 另一个页面标题
URL: https://example.com/b
摘要: ...
```

结果前面还会包裹 `_SEARCH_RESULT_PROMPT`，要求模型：

- 只使用提供的来源；
- 使用 `[1]`、`[2]` 等编号作内联引用；
- 在回答末尾附带“参考来源”；
- 不得编造没有提供的来源或链接。

这是一种“把引用规则放入工具上下文”的实现，真正的引用文本仍由模型生成。客户端目前不解析最终回答里的 `[n]`，也不校验编号是否越界；`RichText` 只负责把最终 Markdown/文本展示出来。

### 8.3 用户最终看到什么

搜索中间结果只存在于模型上下文，不直接作为单独列表展示。用户最终看到的是：

- 折叠栏标题/预览中的运行阶段和思考句子；
- 模型最终生成的回答正文；
- 正文中模型按照提示生成的 `[1]` 等引用或“参考来源”文本。

这使界面保持简洁，但也意味着“来源可视化”完全依赖模型遵守提示。如果产品需要可点击的来源卡片、来源数量或可信引用校验，应在 API 层额外向 UI 发出结构化 `sources` 事件，而不是从最终 Markdown 中反向猜测。

---

## 9. `ChatWorker` 和主窗口如何传递业务事件

### 9.1 Worker 的信号模型

`ChatWorker` 是 `QThread` 子类，核心信号为：

```python
chunk = Signal(str)
reasoning = Signal(str)
status = Signal(str)
done = Signal()
failed = Signal(str)
```

`run()` 调用 `api.stream_chat()`，并根据事件 kind 分发：

```python
for kind, text in api.stream_chat(...):
    if kind == "reasoning":
        self.reasoning.emit(text)
    elif kind == "search":
        self.status.emit(text)
    else:
        self.chunk.emit(text)
self.done.emit()
```

这里没有把 API 的复杂 payload 直接暴露给 UI。UI 只接收三类已经归一化的字符串流，API 协议细节被隔离在 `app/api.py`。

### 9.2 响应关闭和停止

Worker 保存当前 requests Response 的引用。主线程调用 `cancel()` 时：

1. 设置停止事件；
2. 在锁保护下取出当前 Response；
3. 关闭 Response；
4. 网络生成器在下一次检查停止事件时返回；
5. `run()` 发现是主动停止时发送 `done`，不是 `failed`。

这套处理避免把用户主动点击停止误报成网络异常，同时减少长轮询/长连接继续占用。

### 9.3 `StreamContext` 的累计方式

主窗口为每一轮请求创建一个 `StreamContext`，其中与本功能最相关的字段是：

```text
conversation_id
bubble
model
effort
thinking
web_search
content
reasoning
stopped
```

三个实时槽函数分别做单向累计：

| 槽函数 | 累计字段 | 调用气泡方法 |
| --- | --- | --- |
| `_on_reasoning(text)` | `context.reasoning += text` | `bubble.set_reasoning(...)` |
| `_on_status(text)` | 无需累计 | `bubble.set_status(text)` |
| `_on_chunk(text)` | `context.content += text` | `bubble.set_content(...)` |

累计后的完整字符串最终写入 assistant 历史记录。正常完成时 `reasoning_content` 为空则保存为 `None`；停止或失败时，如果已经有部分思考/正文，也会保存部分结果并标记 `stopped`，避免已收到的信息丢失。

### 9.4 主窗口在回答开始时做的 UI 切换

`AssistantBubble.set_content()` 会先调用 `begin_answer()`，再把正文渲染计时器安排到下一次 60ms 批处理：

```text
收到第一段 content
  │
  ├─ ReasoningPanel.set_running(False)
  ├─ 停止搜索/思考指示器
  ├─ 隐藏标准模式 status_row
  ├─ 显示 content_view
  └─ 延迟渲染正文 HTML
```

这意味着“第一段正文到达”是从运行态折叠栏进入回答态的主要触发点。搜索状态事件到达时则会把面板重新置为运行态，兼容模型先发工具调用前缀导致的短暂 `begin_answer()`。

---

## 10. 折叠栏的控件结构和职责

### 10.1 AssistantBubble 的纵向结构

助手消息的大致布局是：

```text
AssistantBubble
└── 外层 QHBoxLayout
    ├── DeepSeek avatar
    └── column QVBoxLayout
        ├── meta_label
        ├── reasoning_panel（thinking 或已有 reasoning 时创建）
        ├── status_row（标准模式/无思考内容时使用）
        ├── content_view（RichText）
        └── actions（复制、完成/停止/错误提示）
```

`reasoning_panel` 和 `status_row` 是两种互斥的运行状态承载方式：

- 有深度思考，或已经收到 reasoning：使用 `ReasoningPanel`；
- 标准模式没有思考内容：使用简单的 `status_row`；
- 标准模式也打开联网搜索：`status_row` 的文字会在“正在生成”和“正在联网搜索”之间更新。

### 10.2 ReasoningPanel 的横向标题行

标题行使用 `QHBoxLayout`，子项顺序固定为：

```text
[ThinkingIndicator] [reasonToggle] [ThinkingPreview (stretch=1)]
```

具体参数：

- 面板边距：左 10、上 7、右 10、下 8；
- 标题行间距：6；
- `ThinkingIndicator`：20×20；
- `reasonToggle`：可勾选按钮，文字旁带右/下箭头；
- `ThinkingPreview`：固定高度 20、水平 `Expanding`、最小宽度 0，并占用 stretch factor 1。

`ThinkingPreview` 被设置为 `WA_TransparentForMouseEvents`，因此它只是视觉层，点击区域仍由 `reasonToggle` 负责，避免预览文本覆盖折叠按钮的交互语义。

### 10.3 展开/折叠

`reasonToggle` 是 checkable `QToolButton`：

- 未勾选：箭头向右，`reasoning_view` 隐藏；
- 勾选：箭头向下，只有存在 reasoning 时才显示 `reasoning_view`；
- `reasoning_view` 使用 `RichText`，支持 Markdown/代码/数学内容的富文本展示；
- 切换后发出 `heightChanged`，由上层 `ChatView` 重新计算消息高度并维持滚动位置。

需要区分两种内容：

1. 标题行的 `ThinkingPreview`：只显示完整句子的动画摘录；
2. 展开后的 `reasoning_view`：显示已经累计的完整思考文本。

前者是低成本的运行态提示，后者是可阅读的完整内容，两者不应该共享同一个截断字符串。

---

## 11. “正在深度思考”和“正在联网搜索”的交替机制

### 11.1 交替的真实驱动源

这两个文案的切换由 API 事件和 AssistantBubble.set_status() 驱动，时间轴大致如下：

| 时间/事件 | API 事件 | 面板动作 | 标题 |
| --- | --- | --- | --- |
| 请求刚开始，且开启思考 | 无需等待事件 | ReasoningPanel(running=True)，启动指示器和预览计时器 | 正在深度思考 |
| 模型流出思考内容 | ("reasoning", text) | 累积 reasoning，更新完整思考视图和预览句子 | 正在深度思考 |
| 模型产生工具调用 | ("search", "正在联网搜索…") | set_phase(searching)，重启运行态 | 正在联网搜索 |
| 工具执行完成 | ("search", "已获取…") 或 ("search", "未找到…") | 当前字符串兼容逻辑将其视为非搜索前缀 | 带思考面板时恢复为正在深度思考，标准模式显示正在生成 |
| 模型开始输出最终正文 | ("content", text) | begin_answer()，停止指示器和预览，显示回答 | 不再显示运行标题 |
| 正常完成/停止/失败 | done 或 failed | 收尾、保存记录、停止全部动画 | 已完成深度思考或错误/停止状态 |

这里的“交替”不是单独启动两个控件再互相隐藏，而是一个面板内部的 phase 状态变化：

```text
THINKING_PHASE
      │
      │ status: 正在联网搜索…
      ▼
SEARCHING_PHASE
      │
      │ content 到达
      ▼
回答阶段（面板停止）
```

搜索工具可能执行多轮，因此状态路径可以重复：

```text
思考 → 搜索 → 思考/生成提示 → 搜索 → 思考/生成提示 → 最终回答
```

每收到一条新的“正在联网搜索…”状态， set_status() 都会调用 set_running(True)，而不是只改文字。这样可以覆盖一个边界情况：模型先发送了工具调用前缀，set_content() 暂时调用了 begin_answer()，随后 API 才发出搜索状态；搜索状态必须能够重新启动指示器和预览动画。

### 11.2 有思考和无思考两条 UI 分支

| 请求模式 | 折叠栏是否创建 | 运行态组件 | 搜索状态文案 |
| --- | --- | --- | --- |
| 深度思考 | 创建 ReasoningPanel | 面板标题、旋转图标、思考预览 | 面板阶段切换 |
| 标准模式，无联网搜索 | 不创建面板 | status_row | 正在生成 |
| 标准模式，有联网搜索 | 通常仍没有面板，直到收到 reasoning | status_row | 正在联网搜索 / 正在生成 |
| 标准模式收到 reasoning | 动态插入 ReasoningPanel，位置在 status_row 之前 | 面板阶段切换 | 同上 |

这也是为什么 AssistantBubble 不能只在构造函数里决定 UI：标准模式虽然没有思考面板，但联网搜索事件仍然需要给用户反馈；而某些兼容网关可能在运行中才开始提供 reasoning 字段，此时面板需要动态创建。

### 11.3 为什么不使用 UI 固定定时轮换标题

预览句子的定时器只负责文字动画，不负责业务阶段。业务阶段由网络流决定，原因是：

- 模型是否搜索、搜索几轮、何时返回工具调用都不可预先确定；
- 搜索服务耗时不固定，固定时间轮换会出现“还在搜索却显示思考”或“已经回答却显示搜索”；
- 状态与真实请求生命周期绑定，停止、失败和重试时更容易收口；
- ThinkingPreview 可以在标题阶段不变时继续循环句子，两类时间尺度彼此独立。

因此应把“状态机时间”和“视觉动画时间”分开维护：前者由事件驱动，后者由 QElapsedTimer 驱动。

---

## 12. 折叠栏思考预览的句子和动画实现

### 12.1 为什么只显示完整句子

reasoning 是流式到达的，最后一个片段经常停在半句话。折叠栏的预览空间很窄，如果每收到几个字就改变文案，会产生跳动、半句和难以阅读的片段。

_extract_complete_sentences(text) 只返回已经识别到句末边界的句子，未完成的尾部保留在完整 reasoning 中，但不放入预览。这样有两个好处：

- 预览始终是语义相对完整的短句；
- 展开折叠栏时仍可看到原始累计的完整思考文本，不会因为预览过滤而丢数据。

### 12.2 句末识别规则

当前实现的规则如下：

1. 中文句末：。、！、？；
2. ASCII 句末：!、?；
3. ASCII 的句点只有在后面是空白或文本结束时才视为句末；
4. 句点前缀是纯数字时不作为边界，用于规避 1. 这类编号；
5. 句点后面可以先跟引号、括号等闭合符号，再判断下一个字符；
6. 连续的句末符号会被归并，例如 ？！；
7. 句末后的引号/括号会并入当前句子，例如“结论。”；
8. 仅换行不构成句末；
9. 没有句末标点的最后一段不会被返回。

实现使用按 Python 字符索引扫描，而不是正则切分，因此能处理“句末标点 + 闭合符号 + 下一句”的组合。

示例：

```python
_extract_complete_sentences(
    "第一句。第二句！尚未完成"
)
# ["第一句。", "第二句！"]
```

目前没有把分号、冒号、单独换行或 Markdown 列表项当作完整句子边界。这是有意的：预览目标是轮播可读句子，而不是复刻完整段落排版。

### 12.3 预览状态

ThinkingPreview 维护以下状态：

| 字段 | 作用 |
| --- | --- |
| _sentences | 当前 reasoning 中所有已完成句子的快照 |
| _current_index | 当前轮播句子的索引 |
| _current_sentence | 当前完整句子 |
| _visible_characters | 当前已经绘制的字符数 |
| _running | 是否处于运行态 |
| _elapsed | 当前句子循环的高精度计时器 |
| _timer | 16ms 的 Qt 精确定时器 |

sentences、current_sentence、visible_text 以只读 property 形式提供给测试和调试；真实绘制直接使用内部状态。

### 12.4 句子更新时如何避免跳变

每次 set_reasoning() 都会重新提取完整句子。如果句子列表没有变化则直接返回，避免无效重绘。

如果列表变化：

- 当前句子仍然存在：保留它的索引，避免新 reasoning 到达时突然跳到另一句；
- 当前句子已经不在列表：把索引限制在新列表范围内，并保留不超过新句长的可见字符数；
- 新列表为空：清空当前句子、可见字符数和计时器；
- 面板仍在运行且计时器未激活：重新开始当前轮播周期。

这是一种轻量的“按句子内容保持选择”策略，比每次重新从第一句开始更符合流式内容不断增长的直觉。

### 12.5 0.5 秒输入、0.5 秒停留、每秒换句

核心常量：

```python
FRAME_INTERVAL_MS = 16
TYPING_DURATION_MS = 500
CYCLE_DURATION_MS = 1000
```

一个句子的时间轴是：

```text
0ms                         500ms                       1000ms
│----------------------------│----------------------------│
从左向右逐字显示              完整句子静止显示               清空并开始下一句
```

每个 16ms tick 通过 QElapsedTimer.elapsed() 取得真实经过时间，而不是简单地每帧加固定增量：

- elapsed < 500ms：可见字符数为 ceil(sentence_length * elapsed / 500)；
- 500ms <= elapsed < 1000ms：显示全部句子；
- elapsed >= 1000ms：索引加一（到末尾后循环），清空可见字符并重启周期。

使用真实 elapsed 的重要性在于：系统调度可能让某个 timer tick 晚到，动画不会因为丢帧而永久变慢，下一帧会按实际时间追上目标进度。

### 12.6 60fps 目标和重绘策略

Qt 定时器间隔为 16ms，配合 Qt.TimerType.PreciseTimer，目标频率约为 62.5 tick/s，视觉目标接近 60fps。动画不使用 sleep()，不会阻塞 UI 线程。

_advance() 只有在可见字符数发生变化时才调用 update()。因此：

- 打字阶段会随着可见字符变化重绘；
- 静止阶段不会每帧做无意义重绘；
- 切换句子时立即刷新；
- stop()、set_reasoning() 清空或更新内容时立即刷新。

这是一种“高频时钟 + 最小必要绘制”的实现。ThinkingIndicator 则在每个 16ms tick 更新角度并重绘，以保持旋转弧的连续性。

需要注意，Qt timer 的 16ms 是调度目标而不是硬实时承诺；当主线程被其他耗时操作阻塞时，任何 QWidget 动画都会降帧。当前代码已经把网络和搜索放到 Worker，降低了最主要的阻塞来源。

### 12.7 右侧渐变淡化

预览控件使用 QSizePolicy.Expanding 占据标题行剩余空间，绘制时：

1. 先用 QPainter 的 TextAntialiasing 绘制 visible_text；
2. 对控件整个矩形做 clip，超出宽度的文字不会溢出到折叠按钮或面板外；
3. 如果完整句子的像素宽度大于控件宽度，在最右侧覆盖一个从透明到 reason_bg 的线性渐变；
4. 渐变宽度最多 34px，同时不小于 12px，并受 width // 5 限制。

渐变使用面板背景色作为不透明端，而不是把文字颜色渐变到透明。这样右侧看起来像被背景自然吃掉，能明确暗示“后方还有内容”，同时不会留下半截高对比字符。

### 12.8 完成态为什么仍要保留空槽位

ThinkingPreview 的水平策略是 Expanding。如果思考结束时调用 hide()，QBoxLayout 会移除它参与空间分配，剩下的指示器和 toggle 可能被布局重新居中，造成图标和“已完成深度思考”从左侧跳到折叠栏中央。

当前 ReasoningPanel.set_running(False) 的处理是：

```python
self.indicator.stop()
self.toggle.setText("已完成深度思考")
self.preview.stop()
self.preview.show()
```

stop() 清空文字并停止动画，show() 保留空的 expanding item。于是：

- 完成后不显示旧句子；
- 图标和文字仍保持运行态时的左侧锚点；
- 右侧保留可伸缩空白；
- 不需要用硬编码 spacer 宽度模拟布局。

这也是折叠栏完成态布局稳定的关键细节。

---

## 13. 接口返回内容如何被合理分层显示

### 13.1 三类内容的边界

整个链路明确区分：

| 内容 | 来源 | 是否立即展示 | 展示位置 |
| --- | --- | --- | --- |
| reasoning | reasoning_content / reasoning | 是，经过标签清理后立即进入预览和完整思考缓存 | 折叠栏预览、展开后的 reasoning_view |
| 工具调用参数 | tool_calls[].function.arguments | 否 | 只用于客户端执行工具和回填模型上下文 |
| 搜索/网页工具结果 | role="tool" content | 否 | 只进入后续模型上下文 |
| final content | content | 是，但以 60ms 批处理频率渲染 | 回答正文 RichText |
| API 错误 | error 或异常 | 作为错误状态处理 | 错误气泡或部分回答下方的错误提示 |

这样可以避免把机器协议、搜索原始结果或内部标记直接展示给用户。

### 13.2 reasoning 的两条渲染路径

AssistantBubble.set_reasoning() 先把 reasoning 累加到内部缓存，然后启动一次性 80ms 计时器；计时器到期后调用 ReasoningPanel.set_reasoning()：

```text
reasoning signal
    │
    └─ 80ms 后由同一个批处理同时更新 ThinkingPreview 和 reasoning_view
```

面板内部的 set_reasoning() 会同时：

- 调用 preview.set_reasoning(reasoning)；
- 调用 to_html(reasoning, dark=...)；
- 将生成的 HTML 设置给 reasoning_view。

80ms 的批处理主要减少流式输出时反复解析 Markdown、调整高度和触发滚动的次数；预览和完整思考视图会在这次批处理里使用同一份最新 reasoning 更新。

### 13.3 content 的批量渲染

AssistantBubble.set_content() 每次只更新 _plain 和 _content_dirty，然后启动单次 60ms content_timer。真正渲染在 _render_content()：

```python
if not self._content_dirty:
    return
self._content_dirty = False
self.content_view.set_html(to_html(self._plain, dark=...))
```

这个节流机制避免每个 token 都重新设置 HTML。RichText 还会根据内容是否包含 data-tex= 或 data-code-block= 选择：

- 普通内容：QTextBrowser，关闭滚动条并按文档高度自适应；
- 数学/代码块内容：_MathWebView，等待网页渲染回报内容高度。

无论使用哪种渲染器，高度变化都会发出 heightChanged，上层重新计算消息尺寸。

### 13.4 工具调用前缀的处理

搜索轮中 round_content 不会在检测到工具调用后立即转发。这样“我先搜索一下”“让我查一下”不会作为最终回答先渲染。

如果本轮没有工具调用，round_content 会按接收顺序作为 content 事件释放；如果本轮存在工具调用，它只保留在 assistant 工具消息上下文中，供下一轮模型理解，不会污染最终回答气泡。

这是内容合理显示的一个重要边界：展示层不需要知道某一段 content 是“最终回答”还是“工具调用前缀”，API 编排层在进入 UI 之前已经做了判定。

### 13.5 内部协议清理

_strip_zenith() 负责处理某些兼容网关可能返回的：

- <zenith_parameter>；
- <zenith_invoke>；
- <zenith_calls>；
- 紧邻这些块的内部 reasoning / response 标记。

清理策略只在文本包含 <zenith 时触发，并且不会把普通英文短语如 response time 误当成内部标签。清理后为空的片段不向 Worker 发送，因此 UI 不会出现空白刷新或协议残片。

### 13.6 历史消息恢复

历史消息不是流式状态，不会重新启动动画。MainWindow.on_select_conversation() 会读取：

- content；
- reasoning_content；
- thinking；
- web_search 等元数据。

然后使用 ChatView.add_assistant(..., streaming=False) 创建已完成气泡。若有思考内容，折叠栏可以展开查看完整 reasoning，但 ThinkingIndicator 和 ThinkingPreview 不处于运行态。这样历史页面不会在每次打开时重复播放“正在思考”。

---

## 14. 请求完成、停止、失败和主题切换的生命周期

### 14.1 创建阶段

发送消息后，主窗口先写入 user message，再创建：

1. 用户气泡；
2. AssistantBubble(streaming=True)；
3. StreamContext；
4. ChatWorker；
5. Worker 信号连接；
6. 网络线程启动。

如果启用深度思考，助手气泡构造时就创建并启动 ReasoningPanel。此时还没有句子，预览保持空白，但指示器已经开始旋转。

### 14.2 正常完成

done 到达后：

1. 主窗口清空当前 StreamContext；
2. 恢复模型选择框和输入面板；
3. 将累计的正文、reasoning、模型、思考强度、联网搜索和 stopped 状态保存为 assistant message；
4. 调用 bubble.finish(false)；
5. 停止预览、指示器和延迟渲染计时器；
6. 刷新消息尺寸、侧边栏标题和输入焦点。

finish() 会先立即渲染尚未刷新的 reasoning/content，再设置完成态，防止最后一个 60ms/80ms 批处理还没触发就丢掉尾部文字。

### 14.3 用户主动停止

点击停止时，StreamContext.stopped = True，再调用 Worker 的 cancel()。Worker 结束后仍走 done，主窗口保存已经收到的部分内容，并将气泡标记为“已停止”。

停止不被当成网络失败，因此不会额外替换成错误气泡；已经存在的部分回答和思考仍然可展开、可复制。

### 14.4 网络/模型失败

如果发生 APIError 或未预期异常：

- 已经收到 reasoning 或 content：保留部分 assistant message，调用 bubble.fail("生成中断：...")；
- 一点内容都没有：移除空的 streaming bubble，添加独立错误气泡；
- 无论哪条路径，都恢复输入状态并刷新侧边栏。

AssistantBubble.fail() 会先执行普通 finish() 收尾，再把错误信息放在完成区域，并使用主题的 danger 色。

### 14.5 控件销毁

AssistantBubble.dispose() 会停止：

- content/reasoning 延迟计时器；
- 标准模式 status indicator；
- reasoning panel 的 indicator；
- ThinkingPreview；
- RichText 的 QTextBrowser/WebEngine 资源。

ChatView.clear() 在移除气泡前调用 dispose()，避免 QTimer 回调或 Chromium surface 在父控件删除后继续访问已失效对象。

### 14.6 主题切换

apply_theme() 会同时更新：

- 面板背景 reason_bg；
- 思考/状态文字颜色；
- 旋转指示器 accent；
- 预览文字颜色；
- 折叠箭头图标；
- reasoning/content 富文本背景和前景色。

如果主题变化导致 reasoning 的 HTML 需要重新生成，ReasoningPanel 会清空 _rendered_reasoning 缓存，再按当前主题调用 set_reasoning()。预览仍复用同一套句子状态，不会因为切换主题重置轮播进度。

---

## 15. 折叠栏和消息列表的尺寸、滚动交互

### 15.1 固定高度与动态高度的组合

标题行中的 ThinkingPreview 固定为 20px 高，不参与面板高度增长；展开的 reasoning_view 和最终 content_view 根据富文本文档高度动态调整。

RichText._set_content_height() 只有在高度变化超过 1px 时才发出 heightChanged，避免字体测量的小数抖动导致布局频繁刷新。

### 15.2 ChatView 如何响应内容变化

主窗口在 _on_reasoning() 和 _on_chunk() 中调用 chat_view.message_updated(bubble)。该方法：

1. 根据 viewport 宽度重新应用消息固定宽度；
2. 调用 scroll_to_bottom()；
3. 只有在用户仍处于跟随输出状态时才真正滚到底部。

面板展开/折叠、富文本高度变化和 WebEngine 内容完成，也会通过 layoutHeightChanged 触发同一个更新路径。

### 15.3 用户向上查看历史时

ChatView 会监听垂直滚动条。如果用户距离底部超过 10px，关闭自动跟随并停止滚动计时器；后续流式内容仍然更新气泡，但不会把用户强行拉回底部。用户点击回到底部/跟随按钮后，再恢复自动滚动。

这使 60fps 预览动画不会带来滚动位置跳动：预览只在固定高度控件内重绘，不会因每一帧改变布局高度。

### 15.4 消息宽度

每条消息宽度计算为：

```python
width = min(LANE_MAX_WIDTH, max(420, viewport_width - 56))
```

因此折叠栏实际可用的 ThinkingPreview 宽度会随窗口变化，但有最小消息宽度和最大阅读宽度。窗口变窄时，右侧渐变负责处理句子截断；窗口变宽时，expanding 预览槽位自动获得更多空间。

---

## 16. 当前实现的边界、潜在问题与演进建议

下面区分“当前已经实现的行为”和“如果继续产品化建议补强的地方”，避免把建议误读为现有代码已经具备的能力。

### 16.1 阶段事件仍然是字符串

当前 ChatWorker.status 只发送 str，AssistantBubble.set_status() 用 startswith("正在联网搜索") 判断是否为搜索阶段。结果是“已获取…正在生成回答…”和“未找到…”会落入思考/生成分支。短期兼容成本低，但文案和状态耦合，容易产生标题语义不完全准确的问题。

建议演进为结构化事件，例如：

```python
{
    "phase": "searching" | "generating" | "thinking",
    "message": "已获取 3 条结果，正在生成回答…",
}
```

UI 使用 phase 决定图标/标题，使用 message 决定辅助文案，就不需要依赖中文前缀。

### 16.2 搜索来源没有单独传给 UI

来源当前只以 tool message 形式存在于模型上下文，最终 UI 看不到结构化的来源列表。若要增加来源卡片、来源数量、域名 favicon、引用点击定位或引用合法性校验，需要新增独立的 sources 信号和持久化字段。

否则只能把最终正文中的 [1] 等文本当普通 Markdown 处理，不能可靠地确认它对应哪个 URL。

### 16.3 引用提示不是强校验

_SEARCH_RESULT_PROMPT 要求模型只引用提供的来源，但客户端没有：

- 验证最终引用编号是否存在；
- 删除越界编号；
- 将 [1] 自动替换为来源超链接；
- 对来源内容做可信度或时效性判断。

这属于“提示约束”而不是“协议约束”。如果引用准确性是硬要求，应在模型输出后增加结构化引用解析/校验层。

### 16.4 网页工具的 SSRF 保护不完整

如第 7 节所述，当前只做显式回环地址拦截。对于真正暴露给不可信输入的环境，应该补充逐跳重定向检查、DNS/IP 分类、端口限制和请求体上限。

### 16.5 句子扫描的复杂度

每次 reasoning 更新都会对累计全文重新扫描 _extract_complete_sentences()，时间复杂度近似为 O(n)。普通思考长度下没有问题，但如果 reasoning 很长且 token 到达很快，建议未来维护扫描游标或增量句子解析器，只从上次未完成尾部继续处理。

### 16.6 字符动画不是按视觉宽度

当前打字进度按 Python 字符数计算，未按像素宽度或 Unicode grapheme cluster 计算。因此：

- 中英文混排时，每个字符占用宽度不同，视觉速度不完全恒定；
- emoji、组合音标或某些复杂脚本可能在 code point 中间切开；
- 长句的字符数与用户感受到的动画时长线性相关。

如需更自然的效果，可以基于 QFontMetricsF.horizontalAdvance() 做按像素进度，或先按 grapheme cluster 分段，再按视觉宽度插值。

### 16.7 没有无障碍/减弱动画开关

当前动画会自动启动，尚未读取系统“减少动态效果”偏好，也没有用户设置。后续可以：

- 对 reduced-motion 环境直接显示完整当前句；
- 保留指示器静态状态；
- 允许在设置中关闭文字轮播；
- 让屏幕阅读器读取稳定标题，而不是把每一帧变化当成新内容。

### 16.8 工具调用兼容性

当前 tool_calls 参数按字符串拼接，最后统一解析；这是兼容流式 delta 的必要做法。仍需注意：

- 不同网关可能把 tool_calls 放在 message 而非 delta；
- 工具调用 index 缺失时当前代码默认使用 0；
- 未识别的工具名会落入查询工具分支，使用 _tool_queries() 尝试执行搜索；
- 非 JSON 参数会变成空参数并回传“没有可用结果”，不会直接崩溃。

如果接入更多工具，建议对未知名称显式报错，而不是隐式当作 web_search 处理。

---

## 17. 状态机与关键不变量

### 17.1 API 层不变量

联网搜索循环应始终满足：

1. 每个 tool_call 都有一个对应的 tool message；
2. 流式 arguments 必须合并完成后再解析；
3. 来源编号在同一个请求内稳定；
4. 达到最大搜索轮次后必须退出工具循环；
5. stop 事件一旦设置，不再启动新的网络或搜索操作；
6. 最终无工具回答使用同一套 reasoning/content 清理逻辑。

### 17.2 UI 层不变量

折叠栏应始终满足：

1. 运行中只有一个当前 phase；
2. ThinkingIndicator 的 running 状态和面板运行态一致；
3. 标题预览不改变面板布局高度；
4. reasoning_view 是否可见只由 toggle 和 reasoning 是否为空决定；
5. 完成态停止动画，但保留 preview 的 expanding 布局槽位；
6. streaming 结束后，不再接受新的 reasoning/status/content 更新；
7. 所有延迟 timer 在 dispose 时停止。

### 17.3 推荐状态图

可以把当前行为画成以下状态图，作为后续重构或补测试时的基准：

```text
                    ┌─────────────────────┐
                    │  THINKING / running │
                    └─────────┬───────────┘
                              │ search status
                              ▼
                    ┌─────────────────────┐
                    │ SEARCHING / running │
                    └──────┬───────┬──────┘
                           │       │ tool result status
                           │       ▼
                           │  THINKING / generating hint
                           │
                           │ final content
                           ▼
                    ┌─────────────────────┐
                    │ ANSWERING / stopped │
                    └──────┬───────┬──────┘
                           │ done  │ failed/stopped
                           ▼       ▼
                    ┌──────────┐ ┌──────────────┐
                    │ COMPLETED│ │ PARTIAL/ERROR│
                    └──────────┘ └──────────────┘
```

其中“tool result status”在当前实现会通过字符串前缀映射到 thinking/generating 分支；如果将来引入显式 phase，应把它单独命名为 GENERATING。

---

## 18. 测试覆盖和验证方式

### 18.1 API 单元测试

tests/test_api.py 已覆盖的重点包括：

- reasoning/content 分流；
- 普通模式和深度思考模式下 tools、tool_choice 的请求体；
- DuckDuckGo Lite 链接和摘要解析；
- 搜索提示词和引用格式；
- 搜索流状态顺序；
- 工具调用前缀不泄漏为最终正文；
- zenith 内部标签清理；
- 网页正文提取；
- loopback URL 拒绝；
- 多轮搜索时全局引用编号保持稳定。

这些测试同时固定了业务协议和展示层依赖的事件顺序，防止只改 UI 后破坏工具循环。

### 18.2 UI 单元测试

tests/test_ui.py 的折叠栏相关测试覆盖：

- 默认思考模式创建折叠栏且初始运行；
- 运行中 indicator 处于 active；
- 思考结束后 indicator 停止、正文可见；
- preview 在完成后仍然存在；
- 完成前后 indicator/toggle 的左侧位置不变，防止布局重新居中；
- 完整句子提取会忽略流式未完成尾部；
- 16ms PreciseTimer；
- 首句在约 0.5 秒内完成输入，约 1 秒切换下一句；
- stop 后 timer 停止；
- 搜索状态可切换标题，工具调用前缀后可重新进入运行态；
- 标准模式使用 status_row。

### 18.3 推荐本地验证命令

项目当前可以用下面的命令进行静态、差异和完整测试检查：

```bash
ruff check app tests
git diff --check
QT_QPA_PLATFORM=offscreen \
QTWEBENGINE_CHROMIUM_FLAGS=--disable-gpu \
python -m unittest discover -s tests -v
```

其中 offscreen 用于没有桌面显示器的测试环境，禁用 GPU 可以减少 Qt WebEngine 在 CI/沙盒环境中的不确定性。动画测试使用 Qt 事件循环等待 timer，不应该用阻塞 sleep 替代。

---

## 19. 修改这部分业务时的建议切入点

### 19.1 只改文案或阶段表现

优先修改：

- ReasoningPanel._RUNNING_LABELS；
- AssistantBubble.set_status() 的 phase 判定；
- API 层 search 状态事件。

不要在 ThinkingPreview 中判断“是否正在搜索”，因为预览组件只应该处理句子动画。

### 19.2 修改动画节奏

只调整 ThinkingPreview 的：

- FRAME_INTERVAL_MS；
- TYPING_DURATION_MS；
- CYCLE_DURATION_MS；
- _advance() 的进度函数；
- paintEvent() 的渐变宽度和字体绘制。

同时更新 tests/test_ui.py 的等待窗口和节奏断言。不要在动画中加入网络调用或长耗时 Markdown 渲染。

### 19.3 增加新的搜索工具

建议按以下顺序：

1. 在 app/api.py 增加工具 schema；
2. 增加参数提取函数，明确非法参数的行为；
3. 增加执行函数和统一结果格式；
4. 在 stream_search_function() 按工具名分支；
5. 统一注册来源和生成 tool message；
6. 为多轮、并行调用、异常和停止写测试；
7. 最后再考虑 UI 是否需要新的 status 或 sources 事件。

### 19.4 增加可视化来源

不要从最终回答字符串中猜来源。建议把 API 事件升级为带类型的数据：

```python
("sources", [
    {"number": 1, "title": "...", "url": "...", "snippet": "..."},
])
```

Worker 可以增加 sources 信号，StreamContext 保存 sources，AssistantBubble 再决定是否渲染来源区域。这样引用编号、点击动作和持久化才能保持一致。

### 19.5 改造状态字符串

建议保留人类可读 message，但增加显式 phase：

```python
SearchStatus(
    phase="searching",
    message="正在联网搜索…",
    result_count=0,
)
```

主窗口/气泡只根据 phase 做状态转换，根据 message 做展示；这样可以消除中文文案改变导致的业务回归。

---

## 20. 总结：当前实现的业务闭环

当前项目已经形成一条完整闭环：

1. 输入面板决定本轮是否开启深度思考和联网搜索；
2. API 层把联网搜索转换为模型可请求的工具；
3. SSE 层将 reasoning、content、tool_calls 分开；
4. 客户端执行搜索和网页提取，并把结果按稳定编号回填；
5. 工具循环最多三轮，之后强制生成最终答案；
6. Worker 把协议细节压缩为 reasoning/status/content 三类 UI 事件；
7. MainWindow 用 StreamContext 累积并持久化；
8. AssistantBubble 将思考、搜索、回答分到不同展示区域；
9. ReasoningPanel 用 phase 响应真实业务阶段；
10. ThinkingPreview 只轮播完整思考句子，并用 500ms 打字 + 500ms 停留的视觉节奏呈现；
11. 16ms 精确定时器、QElapsedTimer、文本裁剪和右侧渐变共同保证预览的轻量与连续；
12. 完成态保留空的 expanding 预览槽位，避免布局重算导致图标和文字跳到中央；
13. 停止、失败、历史恢复、主题切换和销毁流程均有明确收口。

因此，后续最值得优先演进的不是继续堆叠字符串文案，而是：

- 将 status 改为结构化 phase/message 事件；
- 将来源作为独立结构化数据传到 UI；
- 加强网页读取的重定向和私网地址安全校验；
- 对超长 reasoning 使用增量句子解析；
- 为动画加入 reduced-motion 和 Unicode grapheme 处理。

这些改动可以在不破坏现有折叠栏视觉结构的前提下，提高协议可靠性、引用可解释性和无障碍质量。
